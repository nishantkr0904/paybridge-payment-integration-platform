global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'paybridge-server'
    metrics_path: '/api/metrics'
    static_configs:
      - targets: ['paybridge-api:4000']
